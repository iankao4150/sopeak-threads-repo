# 填好後改名成 .setup.env（機密，勿外流）
THREADS_APP_ID=         # Meta App 的 App ID（App 後台 → 設定 → 基本資料）
THREADS_APP_SECRET=     # App Secret（同上，點顯示）
REDIRECT_URI=https://oauth.pstmn.io/v1/browser-callback   # 要和 App 後台 Threads 設定裡填的「完全一樣」
AUTH_CODE=              # 第一次先留空；拿到授權網址、授權後把 code= 的值貼這裡
